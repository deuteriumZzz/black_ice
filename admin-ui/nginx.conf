server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Client-side routing (React Router) — any unmatched path falls back to
    # index.html so deep links like /identities refresh correctly.
    location / {
        try_files $uri /index.html;
    }
}
