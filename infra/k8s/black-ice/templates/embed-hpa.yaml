{{- if .Values.embed.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: black-ice-embed
  labels:
    {{- include "black-ice.componentLabels" (dict "component" "embed" "Release" .Release) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: black-ice-embed
  minReplicas: {{ .Values.embed.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.embed.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.embed.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
